import { StyleSheet } from 'react-native';

/**
 * Estilos simplificados para CreateOrder.
 * Responsabilidade única: layout principal da tela.
 * Estilos específicos movidos para componentes individuais.
 */
export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  
  keyboardAvoid: {
    flex: 1,
  },
});